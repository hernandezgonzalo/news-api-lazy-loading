import { makeStyles } from "@material-ui/core";

export const AppStyles = makeStyles({
  newsGrid: {
    margin: "auto"
  },
  newsWrapper: {
    height: "100vh",
    overflow: "auto",
    overflowX: "hidden"
  }
});
